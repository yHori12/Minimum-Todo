<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.y_hori.minimum_todo.ui.main.MainViewModel" />
        <import type="android.view.View"/>
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/coodLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainFragment">

        <ProgressBar
            android:id="@+id/progress_bar"
            android:visibility="@{viewModel.shouldShowProgress? View.VISIBLE : View.GONE  }"
            style="@style/style_progress_bar" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/task_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/list_item_new_task" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab"
            style="@style/Widget.Design.FloatingActionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="@dimen/margin_16"
            android:background="@color/colorAccent"
            android:elevation="@dimen/margin_16"
            android:padding="12dp"
            android:text="Todoを追加"
            android:textColor="@color/color_white"
            android:textSize="18sp"
            app:cornerRadius="24dp"
            app:icon="@drawable/ic_notifications_active_24px" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
